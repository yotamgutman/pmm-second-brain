# Sample Outputs

Pre-generated outputs from `agent.cli` running in `--mock` mode (no API key needed) against the vault in this repo. These are the same markdown the Streamlit app renders — useful for browsing without running anything.

- `battle_card_opschain.md` — `python -m agent.cli battle-card OpsChain --mock`
- `release_note_autosync.md` — `python -m agent.cli release-note --mock`
- `drift_check.md` — `python -m agent.cli drift-check --mock` (the headline feature — see the root README)
