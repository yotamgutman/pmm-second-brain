# Generated Battle Cards

This folder is the output destination for battle cards generated by the PMM agent (`agent/tools.py` → `generate_battle_card`).

It's empty by default in the repo — run the agent to populate it, or see `examples/sample_outputs/` for a pre-generated sample.
